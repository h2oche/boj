#include <stdio.h>
#include <vector>
#include <algorithm>

using namespace std;

int main(void)
{
    int n;
    scanf("%d\n", &n);

    for(int i = 0 ; i < n ; i += 1) {
        int x, y, z;
        scanf("%d %d %d\n", &x, &y, &z);
    }
    return 0;
}