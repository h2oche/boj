#include <iostream>
#include <vector>
#include <algorithm>
#define endl '\n'
using namespace std;

long long n,m;

int main()
{
  ios::sync_with_stdio(false);
  cin.tie(NULL);
  cin >> n >> m;
  cout << n * m / 2;
  return 0;
}
